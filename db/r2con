R2PM_BEGIN

R2PM_DESC "[r2con] The official radare2 conference"
R2CON_LINK="https://radare.org/con/"

R2PM_INSTALL() {
	xdg-open ${R2CON_LINK} > /dev/null 2>&1
	[ $? != 0 ] && open ${R2CON_LINK} > /dev/null 2>&1
}

R2PM_UNINSTALL() {
	# you cant uninstall r2con
	:
}

R2PM_END
