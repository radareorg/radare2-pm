R2PM_BEGIN

R2PM_GIT "https://github.com/Yara-Rules/r2yara"
R2PM_DESC "[syspkg] r2 module for yara. Powerful of r2 functionalities in Yara"

R2PM_INSTALL() {
    # Yara
    git clone https://github.com/VirusTotal/yara

    # R2 module for Yara
    cp r2.c yara/libyara/modules/r2.c
    cp dist/yara-3.6.3/libyara/modules/module_list yara/libyara/modules/module_list
    cp dist/yara-3.6.3/libyara/Makefile.am yara/libyara/Makefile.am
    cp dist/yara-3.6.3/configure.ac yara/configure.ac
    echo "AM_CFLAGS += @DOLLAR_SIGN@(shell pkg-config --cflags r_socket)" >> yara/Makefile.am
    echo "LIBS += @DOLLAR_SIGN@(shell pkg-config --libs r_socket)" >> yara/Makefile.am

    # libjansson
    wget http://www.digip.org/jansson/releases/jansson-2.7.tar.gz
    tar -xzvf jansson-2.7.tar.gz
    cd jansson-2.7
    ./configure --prefix="${R2PM_PREFIX}"
    ${MAKE}
    ${R2PM_SUDO} ${MAKE} install
    cd ..

    # Compile Yara
    cd yara
    export EXT_SO=$(r2 -hh|grep LIBEXT|awk '{print $2}')
    ./bootstrap.sh
    ./configure --prefix="${R2PM_PREFIX}" --enable-cuckoo || R2PM_FAIL "Configure"
    ${MAKE} || R2PM_FAIL "Build"
    ${R2PM_SUDO} ${MAKE} install || R2PM_FAIL "Install"
}

R2PM_UNINSTALL() {
    ${R2PM_SUDO} ${MAKE} uninstall
}

R2PM_END
