R2PM_BEGIN

R2PM_GIT "https://github.com/trufae/mai"
R2PM_DESC "My Artificial Intelligence Toolkit"

R2PM_INSTALL() {
	if [ "${GLOBAL}" = 1 ]; then
		${MAKE} -j
		${SUDO} ${MAKE} install
	else
		echo "Global install only"
		exit 1
	fi
}

R2PM_UNINSTALL() {
	${SUDO} ${MAKE} uninstall
}

R2PM_END
