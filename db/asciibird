R2PM_BEGIN

R2PM_GIT "https://github.com/hamikm/AsciiBird/"
R2PM_DESC "ASCII art Flappy Bird game"

R2PM_INSTALL() {
	make
	cp -f driver "${R2PM_BINDIR}/asciibird"
}

R2PM_UNINSTALL() {
	rm -f "${R2PM_BINDIR}/asciibird"
}

R2PM_END
